export interface CustomerDTO {
  id: number;
  customerType: string;
  documentNumber: string;
  name: string;
  companyName: string;
  email: string;
  phoneNumber: string;
  address: string;
  contacts: string[];
}
